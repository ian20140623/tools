# tmux 跨機共用設定範本（手動抄寫，非自動部署）
# 用法：在每台機器的 ~/.tmux.conf 加入以下內容，存檔後 reload：
#   tmux source-file ~/.tmux.conf      （沒開 tmux 的話下次開自動生效）

# --- prefix 改 C-a（取代預設 C-b）---
# 想送字面 Ctrl-a 給 shell（跳行首）時：連按兩次 C-a（send-prefix）
unbind C-b
set -g prefix C-a
bind C-a send-prefix
